import SuppliesClientPage from '@/components/features/supplies/SuppliesClientPage';

// Esta página renderiza o componente cliente que contém toda a lógica
// para gerenciar insumos, categorias, importação e exportação.
export default function InsumosPage() {
  return <SuppliesClientPage />;
}
